type KAP @model @auth(rules: [{allow: public}]) {
  id: ID!
  BusStop: String!
  TripNo: Int!
  Count: Int!
}

type CLT @model @auth(rules: [{allow: public}]) {
  id: ID!
  BusStop: String!
  TripNo: Int!
  Count: Int!
}

type BOOKINGDETAILS5 @model @auth(rules: [{allow: public}]) {
  id: ID!
  MRTStation: String!
  TripNo: Int!
  BusStop: String!
}
 